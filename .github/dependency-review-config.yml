# Configuration for actions/dependency-review-action
# See https://github.com/actions/dependency-review-action

# Fail on high severity vulnerabilities
fail-on-severity: high

# Allow specific licenses. Note: allow-licenses and deny-licenses are mutually
# exclusive - an allowlist already denies everything else.
allow-licenses:
  - MIT
  - Apache-2.0
  - BSD-2-Clause
  - BSD-3-Clause
  - ISC
  - CC0-1.0

# Private packages are allowed (tap doesn't have private deps)
private-packages-allowed: true